{
  "name": "nodetov2",
  "version": "1.0.0",
  "description": "",
  "main": "api/app.js",
  "scripts": {
    "start": "node api/app.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "http-proxy": "latest",
    "ws": "^8.13.0"
  }
}
